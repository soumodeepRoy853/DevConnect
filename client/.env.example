# Example environment variables for the client app.
# Copy to .env.local for local development and DO NOT commit real secrets.

# Public API base URL (include trailing /api if you use that in server)
NEXT_PUBLIC_API_URL=https://api.example.com/api
